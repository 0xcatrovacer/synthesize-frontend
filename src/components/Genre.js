// import React from 'react'

// function Genre( {title, songs} ) {
//     return (
//         <div>
//             <span className="angleLeft1 sliderButton" onClick={() => handleScroll(-800)} >&#10094;</span>
//             {songs.map((track) => (
//                 <span className="track" key={track.id}>
//               <Link to={track.url}><img className="image" src={track.imageurl} /></Link>
//               <span className="tname">{track.name}</span>
//               <span className="tartist">{track.artist}</span>
              
//              </span> 
             
             
//              ))}

//              <span className="angleRight1 sliderButton" onClick={() => handleScroll(800)}>&#10095;</span>
//         </div>
//     )
// }

// export default Genre
